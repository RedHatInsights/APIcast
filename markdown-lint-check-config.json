{
    "ignorePatterns": [
        {
            "pattern": "^.*.foo"
        },
        {
            "pattern": "^.*.bar"
        },
        {
            "pattern": "^https://docs\\.redhat\\.com"
        },
        {
            "pattern": "^https://access\\.redhat\\.com/documentation"
        }
    ],
    "replacementPatterns": [
        {
            "pattern": "^/",
            "replacement": "{{BASEURL}}/"
        }
    ],
    "retryOn429": true,
    "retryCount": 5,
    "fallbackRetryDelay": "30s"
}
